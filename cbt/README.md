# CBT Content

This folder groups all CBT content used by the app:

- `slideshows`
- `training-videos`
- `audio-books`

Keep the metadata and markdown content inside these subfolders so the web app can fetch all CBT material from one top-level `cbt/` location.
